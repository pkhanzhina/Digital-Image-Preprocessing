from easydict import EasyDict

cfg = EasyDict()

cfg.hue_interval = (41, 81)
cfg.saturation_interval = (4, 205)
cfg.value_interval = (0, 195)

cfg.min_area = 10000
cfg.max_area = None

cfg.blur = True
